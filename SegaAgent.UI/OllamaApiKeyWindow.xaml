<Window x:Class="SegaAgent.UI.OllamaApiKeyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Ollama Connection"
        Width="560"
        Height="390"
        MinWidth="520"
        MinHeight="360"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource SegaBackgroundBrush}"
        Foreground="{StaticResource SegaTextBrush}"
        FontFamily="Segoe UI"
        FontSize="13"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Grid Background="{StaticResource SegaWindowGradient}">
        <Grid.RowDefinitions>
            <RowDefinition Height="58"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#B0060A16"
                BorderBrush="#2840618C"
                BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Margin="18,0,8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="OLLAMA CONNECTION"
                               FontSize="15"
                               FontWeight="Bold"
                               Foreground="{StaticResource SegaTextBrush}"/>
                    <TextBlock Text="Reasoning provider credential"
                               Margin="0,2,0,0"
                               FontSize="9"
                               Foreground="#53738F"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Width="46"
                        Height="38"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#9FB4C9"
                        Cursor="Hand"
                        Click="Close_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Width="11" Height="11" Stretch="Fill"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="1.4"
                          Data="M1,1 L10,10 M10,1 L1,10"/>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="22,20,22,22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="HeadingText"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#ECF7FF"/>
                <TextBlock x:Name="DescriptionText"
                           Margin="0,7,0,0"
                           FontSize="11.5"
                           Foreground="#829BB3"
                           TextWrapping="Wrap"
                           LineHeight="17"/>
            </StackPanel>

            <Border Grid.Row="1"
                    Margin="0,16,0,0"
                    Padding="14,11"
                    CornerRadius="10"
                    Background="#A90A1726"
                    BorderBrush="#4A347FB4"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="API KEY"
                               FontSize="8.5"
                               FontWeight="SemiBold"
                               Foreground="#72D8FF"/>
                    <PasswordBox x:Name="ApiKeyBox"
                                 Margin="0,8,0,0"
                                 Padding="10,8"
                                 Background="#C2081321"
                                 Foreground="#EEF7FF"
                                 BorderBrush="#4A4D83B8"
                                 BorderThickness="1"
                                 PasswordChanged="ApiKeyBox_PasswordChanged"/>
                    <TextBlock Text="Saved to your Windows user environment and reused automatically after Sega or the PC restarts."
                               Margin="0,7,0,0"
                               FontSize="9.5"
                               Foreground="#647E97"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <TextBlock x:Name="WarningText"
                       Grid.Row="2"
                       Margin="0,12,0,0"
                       FontSize="10.5"
                       Foreground="#D2A4FF"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"/>

            <TextBlock x:Name="ValidationText"
                       Grid.Row="3"
                       Margin="0,8,0,0"
                       FontSize="10.5"
                       Foreground="#FF8DA3"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"/>

            <StackPanel Grid.Row="5"
                        Margin="0,18,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="OpenOllamaButton"
                        Content="Open Ollama"
                        MinWidth="104"
                        Margin="0,0,8,0"
                        Padding="14,8"
                        Style="{StaticResource SegaButtonBaseStyle}"
                        Click="OpenOllama_Click"/>

                <Button x:Name="RetryCurrentButton"
                        Content="Retry current"
                        MinWidth="112"
                        Margin="0,0,8,0"
                        Padding="14,8"
                        Style="{StaticResource SegaButtonBaseStyle}"
                        Click="RetryCurrent_Click"
                        Visibility="Collapsed"/>

                <Button x:Name="SaveButton"
                        Content="Save &amp; retry"
                        MinWidth="118"
                        Padding="14,8"
                        Style="{StaticResource SegaPrimaryButtonStyle}"
                        Click="Save_Click"
                        IsEnabled="False"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>